{
  "name": "easy_read",
  "version": "1.0",
  "main": "src/main.js",
  "scripts": {
    "lint": "web-ext lint -s ./src/",
    "build": "web-ext build -s ./src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:chromium": "web-ext run -t chromium --source-dir ./src/",
    "start:firefox": "web-ext run -t firefox-desktop --source-dir ./src/"
  },
  "license": "MIT",
  "devDependencies": {
    "web-ext": "^6.1.0"
  }
}
